
  <% if @logged_user["role"] == "admin" %>
      <div class="text-center">
      <%= link_to " Add new project", new_project_path,action_name: :new %>       |
      <%= link_to "Add new user", new_user_path, action_name: :new %>
      </div>
      <br>

    <div class="row row-grid text-info " style="height: 350px">
      <%= render 'programmers'%>
      <%=render 'projects'%>
      <%=render 'leaders'%>
      </div>
      <br>
      <div class="row row-grid text-info " style="height: 350px">
        <%= render 'notes'%>
      <%= render 'latesttasks' %>

    </div>

  <% elsif @logged_user["role"] == "team leader" %>
      <div class="row row-grid text-info" style="height: 350px">
      <%= render 'programmersforleaders'%>
      <%= render 'myprojects'%>
      </div>
      <br>
      <div class="row row-grid text-info" style="height: 350px">
        <%= render 'notes'%>
      <%= render 'latesttasks' %>
      </div>


  <% elsif @logged_user["role"] == "programmer" %>
      <div class="row row-grid text-info " style="height: 350px">
        <%= render 'myprojects'%>
        <%= render 'latesttasks' %>
      </div>
      <br>
      <div class="row row-grid text-info col-sm-offset-4">
        <%= render 'notes'%>

      </div>
  <%end%>
<br>



