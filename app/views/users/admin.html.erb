<!DOCTYPE html>
<html>
<header >
  <nav class="navbar navbar-inverse">
    <div class="container-fluid">
      <div class="navbar-header">
        <a class="navbar-brand" >
          <p>Project Management App</p>
        </a>
        <p class="navbar-text">Signed in as <%= User.find(session[:user_id]).name%></p>

         </div>
      <a  class="navbar-right " ><%= button_to "Log out", logout_path, :method => :delete,:class=>"btn btn-default btn-sm"%></a>
    </div>
  </nav>
</header>
<body class="bg-success">
<div >
  <p>
  <%= link_to "Add new project", controller: "projects", action: "new", id: @project %>|
  <%= link_to "Add new user", new_user_path, action_name: "new" %>
</p>
</div>
<br>
<div class="row row-grid text-info">
  <div class="col-md-4 ">
    <div class="col-md-12 bg-info ">

    <table class="table ">
      <h4>List of programmers:</h4>
      <thread>
        <tr >
          <th>Name</th>
          <th>Email</th>
          <th></th>
          <th></th>
        </tr>
      </thread>
  <% @user = User.where(role: 'programmer') %>
  <% @user.each do |user| %>
  <tbody>
  <tr>
        <td ><%= user.name %></td>
        <td ><%= user.email %></td>
    <td > <%= button_to 'Edit',edit_user_path(user),:method => "get",:class => 'btn btn-xs btn-warning'%></td>
    <td ><%= button_to 'Delete',user_path(user),method: :delete, data: { confirm: 'Are you sure?'},:class => 'btn btn-xs btn-danger'%></td>
  </tr>
  </tbody>
  <% end %>
  </table>
</div>
  </div >
  <div class="col-sm-4 ">
    <div class="col-md-12 bg-info">

  <table class="table">
    <h4>List of team leaders:</h4>
  <thread>
  <tr>
    <th>Name</th>
    <th>Email</th>
    <th></th>
    <th></th>
  </tr>
  </thread>
  <% @user = User.where(role: 'team leader') %>
  <% @user.each do |user| %>
      <tbody>
      <tr>
        <td ><%= user.name %></td>
        <td ><%= user.email %></td>
        <td > <%= button_to 'Edit',edit_user_path(user), :method => "get",:class => 'btn btn-xs btn-warning'%></td>
        <td ><%= button_to 'Delete',user_path(user),method: :delete, data: { confirm: 'Are you sure?'},:class => 'btn btn-xs btn-danger'%></td>
      </tr>
      </tbody>
  <% end %>
</table>
</div>
</div>

<div class="col-sm-4 ">
  <div class="col-md-12 bg-info">

  <table class="table">
    <h4>List of projects:</h4>
    <thread>
      <tr>
        <th>Name</th>
        <th> </th>
      </tr>
    </thread>
  <% @project = Project.all %>

  <% @project.each do |project| %>
      <tbody>
      <tr>
        <td><%= project.name %></td>
        <td ><%= button_to "Show" ,project_path(project.id), :method => "get", :class => 'btn btn-xs btn-info'%></td>
        <td ><%= button_to "Edit" ,edit_project_path(project.id),:method => "get",:class => 'btn btn-xs btn-warning'%></td>
      </tr>
      </tbody>
  <% end %>
</table>
</div>
</div>
</div>
</body>
</html>